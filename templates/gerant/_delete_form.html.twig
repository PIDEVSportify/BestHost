<form method="post" action="{{ path('gerant_delete', {'Id_gerant': gerant.Idgerant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerant.Idgerant) }}">

    <button class="btn btn-danger unban-btn">Supprimer</button>

</form>
